export function Footer() {
    return (
        <footer>
            <p>
                Screenings · Bookings · Payments · Notifications · Gateway.
                Everything past “Placed” happens on RabbitMQ — the tape above is
                that bus, not a page that polls and calls it one.
            </p>
        </footer>
    );
}
